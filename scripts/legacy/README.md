# scripts/legacy — Development-Only Scripts

Scripts moved here are one-time-use or pre-optimization tools.
They are **not** supported workflows. Use scripts at the repo root instead.

| File | Reason |
|------|--------|
| `generate_all_scenarios.py` | Generates old 34-scenario bank (now 9 gov scenarios) |
| `profile_baseline.py` | Pre-speedup profiling baseline (26.5× improvement already shipped) |

