# OSM-realistic scenario: Athens city center (easy difficulty)
# Requires: data/maps/athens_37.97_23.72.npz (generated by tools/osm_pipeline/)
name: "osm_athens_easy_v1"
domain: "urban"
difficulty: "easy"

# Map / physics
map_size: 500
max_altitude: 10

# These are overridden by the .npz tile data at runtime:
building_density: 0.0   # ignored when map_source is "osm"
building_level: 0       # ignored when map_source is "osm"

# Start/goal policy
start_altitude: 2
safe_altitude: 8
min_start_goal_l1: 100

# Disturbances
wind: "low"
traffic: "low"

# OSM-specific configuration (via escape hatch)
extra:
  map_source: "osm"
  tile_file: "athens_37.97_23.72.npz"
  tile_resolution_m: 1.0
  bbox: [37.97, 23.72, 37.98, 23.73]
